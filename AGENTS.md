When designing or iterating on index.html, always create separate versioned files (v1-, v2-, v3-, etc.) and never overwrite index.html unless explicitly told to.

This rule only applies when the user says "make a v2" / "create a new version" of a design. During normal debugging or coding, edit files directly — don't ask for permission.
